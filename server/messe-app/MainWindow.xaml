<Window x:Class="Herrmann.MesseApp.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Messe App" Height="768" Width="1024"
        WindowState="Maximized"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Status Bar -->
        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="Server wird gestartet..." VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="Orange" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- WebView2 -->
        <wv2:WebView2 x:Name="WebView" Grid.Row="1" />
    </Grid>
</Window>

